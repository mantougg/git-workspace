# Tauri Updater 签名密钥（仅本地构建使用，请勿提交 .env）
#
# 说明：
# - Tauri 构建本身不会自动读取 .env 文件（官方明确），需通过 dotenv-cli 加载，
#   见 package.json 的 "build:signed" 脚本：dotenv -e .env -- tauri build
# - CI 构建由 GitHub Secrets 提供密钥，不依赖此文件。
#
# 使用方法：
#   1. 复制本文件为 .env：copy .env.example .env
#   2. 填入下方两个变量的真实值
#   3. 运行 pnpm build:signed
#
# TAURI_SIGNING_PRIVATE_KEY：私钥文件路径，或私钥内容
#   路径示例（Windows，建议用正斜杠）: C:/Users/<你>/.tauri/gitworkspace.key
#   内容示例：直接粘贴 ~/.tauri/gitworkspace.key 的全部文件内容
# TAURI_SIGNING_PRIVATE_KEY_PASSWORD：生成密钥时设置的密码（未设密码则留空）
TAURI_SIGNING_PRIVATE_KEY=
TAURI_SIGNING_PRIVATE_KEY_PASSWORD=

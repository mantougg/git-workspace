# F-02 Change Set 页面无法返回首页

| 项 | 值 |
|---|---|
| 优先级 | P0 |
| 状态 | ✅ 已完成 |
| 来源 | 2026-08-27 用户反馈问题 2 |
| 关联任务 | T-22 Change Set |

## 问题描述

点击进入 Change Set 页面后，没有任何途径返回首页（无返回按钮 / 面包屑 / 侧边导航入口），只能重启应用或强退。

## 定位线索

- 路由：`src/router/`，Change Set 视图：`src/views/` 下 ChangeSet 相关组件
- 对照其他页面（如 Dashboard / Health）的导航方式，确认是路由未注册返回入口还是布局缺少导航组件

## 修复范围

- [x] Change Set 页面提供明确的返回首页途径（返回按钮 / 面包屑 / 全局导航，与整体布局风格一致）
- [x] 同步排查同类「进去出不来」的页面，一并修复（至少在时间线记录排查结论）

## 验收标准

- [x] 从首页进入 Change Set 后，一步操作即可返回首页
- [x] 浏览器历史式前进/后退（如有）不出现死循环

## 进度

### 状态

- 当前状态：已完成
- 最近更新：2026-08-27 完成

### 时间线

| 日期 | 状态 | 说明 |
|---|---|---|
| 2026-08-27 | ⬜ | 问题录入（拆分自用户反馈） |
| 2026-08-27 | 🟦 | 开始修复 |
| 2026-08-27 | ✅ | 完成：根因=`ChangeSetView.vue` 工具栏缺少返回入口（其余 18 个页面均有 `goBack → router.push({name:"dashboard"})` 模式）。修法=工具栏左侧补「返回」text 按钮（ArrowBackOutline），与 HealthView 等页面同款；同类排查结论：`RepositoryList.vue`（/changes 变更与操作页）同样缺返回按钮，由 F-09h 跟踪，不在本任务重复修复。验证=`pnpm build`（vue-tsc + vite）通过 |

### 子任务清单

- [x] Change Set 增加返回首页导航
- [x] 同类页面导航排查（结论：仅 RepositoryList 缺失，归 F-09h）

# F-08 工作区管理页面

| 项 | 值 |
|---|---|
| 优先级 | P1 |
| 状态 | ✅ 已完成 |
| 来源 | 2026-08-27 用户反馈问题 8 |
| 关联任务 | T-01 Scanner、T-33 Workspace Manifest |

## 问题描述

当前只能新增工作区，没有工作区管理入口。需要一个工作区管理页面，卡片式展示已有工作区。

## 修复范围

- [x] 新增「工作区管理」页面与路由/导航入口
- [x] 卡片展示每个工作区：**名称、目录路径、扫描深度**
- [x] 卡片上的基本管理操作：进入（设为当前并回首页）、编辑（名称 + 扫描深度；路径不可改——改动路径等同于新建工作区，后端 `UpdateWorkspaceRequest` 也不支持改路径）、删除（二次确认，只删配置不动磁盘）
- [x] 新增工作区入口迁移/保留在此页面（页面工具栏保留「添加工作区」复用现有 `WorkspaceManager` 弹窗；首页工具栏加「工作区管理」入口）

## 验收标准

- [x] 工作区管理页列出全部已配置工作区，卡片三项信息（名称/路径/深度）与配置存储一致
- [x] 从首页可进入该页面，且能返回首页（与 F-02 导航修复联动）
- [x] 管理操作（实现的子集）真实生效并持久化

## 进度

### 状态

- 当前状态：已完成
- 最近更新：2026-08-27 完成

### 时间线

| 日期 | 状态 | 说明 |
|---|---|---|
| 2026-08-27 | ⬜ | 问题录入（拆分自用户反馈） |
| 2026-08-27 | 🟦 | 开始修复 |
| 2026-08-27 | ✅ | 完成：新增 `src/views/WorkspaceManageView.vue`（卡片网格：名称/路径/扫描深度标签 + 当前工作区标记；操作=进入/编辑/删除，删除二次确认且明确只删配置不动磁盘；编辑弹窗复用后端 `update_workspace`，仅名称+扫描深度可改，路径不可改——后端 `UpdateWorkspaceRequest` 不支持且改路径语义上等于新建）+ `/workspaces` 路由 + 首页工具栏「工作区管理」入口；`stores/workspace.ts` 补 `updateWorkspace` action（同步 currentWorkspace）；「添加工作区」复用现有 `WorkspaceManager` 弹窗。验证=`pnpm build` 通过，WorkspaceManageView chunk 正常产出；未在运行态点击验证 |

### 子任务清单

- [x] 工作区管理页面与卡片展示
- [x] 管理操作（范围：进入/编辑名称与扫描深度/删除；路径不可改）
- [x] 导航整合（首页入口 + 页内返回）

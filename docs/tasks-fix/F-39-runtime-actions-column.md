# F-39 Runtime 总览操作列：收起重启/构建，放出日志

| 项 | 值 |
|---|---|
| 优先级 | P2 |
| 状态 | ✅ 已完成 |
| 来源 | 2026-09-04 用户反馈（第 2 项） |
| 关联任务 | F-31（删除项危险样式）、F-35（Processes 分页） |

## 需求描述

Runtime 总览 Applications 卡片数据表格操作列：把「重启」「构建」收进
「更多」下拉，把「日志」放出到平铺区。平铺按钮为：启动、停止、日志
（node 工程另有「装依赖」）。

## 实现

- `src/views/RuntimeDashboard.vue` 操作列 render：primary 移除重启按钮、
  新增日志按钮（`openLogs(row.name)`）；`moreOptions` 调整为
  重启 / 构建 / 配置 / 删除；`onMore` 增加 `restart` 分支。

## 验收标准

- [x] 平铺区为 启动/停止/日志（node 行含装依赖），更多含 重启/构建/配置/删除
- [x] 各按钮行为不变（restart 移入 onMore 分支，其余复用原 handler）
- [x] `pnpm build` 通过

## 进度

### 状态

- 当前状态：✅ 已完成
- 最近更新：2026-09-04 修复完成，构建通过

### 时间线

| 日期 | 状态 | 说明 |
|---|---|---|
| 2026-09-04 | ⬜ | 用户反馈录入：操作列收起重启/构建、放出日志 |
| 2026-09-04 | ✅ | 平铺 启动/停止/日志（+node 装依赖），更多收 重启/构建/配置/删除。验证：pnpm build 通过 |
